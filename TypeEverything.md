# Design: 翻译输入法 — Pause-300ms 触发的 OS 级 IME (zh→en hackathon)

Generated by /office-hours on 2026-04-30
Branch: master
Repo: hrdai
Status: **APPROVED**
Mode: Builder (Hackathon / demo)
Spec review: 8.5/10 over 3 adversarial review rounds, 21 issues caught + fixed.

---

## TL;DR (一页纸)

| 项 | 决定 |
|---|---|
| **产品** | Windows OS 级输入法。打拼音 → 显示中文 → 静止 300ms → LLM 替换为英文 → 空格 commit 英文。 |
| **Demo 场景** | 跨语言聊天。我打中文，朋友收英文。 |
| **Demo magic** | 在 5 个不同 Windows app 里 30 秒连切一遍演示普适性。Notepad → Word → Chrome 地址栏 → Discord → VSCode。 |
| **平台** | Win11。不投 Mac/Linux。 |
| **语言对** | zh→en。其它对留 v2。 |
| **Stack** | PIME (Python TSF host) + asyncio + httpx + DeepSeek `deepseek-chat` + Pinyin2Hanzi。 |
| **核心 UX** | Pause-300ms 触发，max-buffer 40 字符强制触发兜底。 |
| **预算** | 48 小时。30 hr 编码 + 18 hr buffer。 |
| **首要风险** | TSF composition rewrite 在 Chrome omnibox / Electron 行为未知。**Hour 1-2 必测**。 |
| **次要风险** | DeepSeek 欧美网络 p50 可能 >500ms。后备：Groq / Cerebras。 |
| **拒绝项** | 浏览器插件、覆盖层、独立聊天 app、本地 LLM、候选词 UI、设置面板、多语言对、Tab tone、产品级 telemetry。 |
| **下一步** | 今天做两个 2 小时验证：(1) PIME 装得上 + 5-app smoke test (2) Pinyin2Hanzi 输出可读。两个都过再写代码。 |

---

## Problem Statement

做一个 Windows OS 级输入法 (IME)，用户用拼音正常打中文，停止输入 300ms 后，composition window 里的中文被 LLM 翻译成英文，按空格 commit 英文。Demo 场景：在跨语言聊天里 (WhatsApp / Discord / iMessage / WeChat web)，我打中文，对方收到自然英文。

约束：
- **真 OS 级 IME**：通过 Windows 标准 IME 列表 (Win+Space 或 Ctrl+Space — 这两个由 Windows 管，IME 不能改) 切到本 IME 后，所有 Windows app 都能用 — 记事本、Word、Chrome 地址栏、Electron app (VSCode / Discord)。本 IME 内部的功能热键 (例如英文/中文模式临时切换) 默认 Ctrl+Shift+T，可在 config.json 覆盖。不是浏览器插件、不是单 app 覆盖层、不是独立聊天 app。
- **Hackathon 时间盒**：48 小时。Demo 友好 > 功能完整。
- **语言对**：仅 zh→en。其它对留 v2。

## What Makes This Cool

**核心 "whoa" 不在翻译质量，在普适性**。Demo 那一刻是：把同一个 IME 在 5 个完全不同的 Windows app 里连续切一遍，30 秒钟内全部表现一致。Notepad → Word → Chrome 地址栏 → Discord → VSCode 注释。**"它在记事本里能用"** 是产品本身。

Pause-300ms 触发提供视觉魔法：你看见自己的中文出现，停一拍，中文活生生变成英文。这一帧是给观众的钩子。

二阶魔法（不在 v1 范围）：双向 shadow channel — 把原文中文用 Unicode tag chars 隐藏在外发英文里，对方装一个浏览器插件就能反向看到中文。"两个人各打母语，谁也不知道对方实际敲的什么"。这是 v2/v3 命题。

## Constraints

- **平台**：Windows 11。macOS / Linux 不在 hackathon 范围。
- **延迟**：用户期望 IME <50ms 响应。LLM 调用 200-1500ms。靠 Pause-300ms 触发把延迟挪到"间歇期"，不阻塞按键流。
- **Stack**：Python 后端 (PIME 提供 TSF 胶水)。无 C++。
- **LLM 主选**：DeepSeek `deepseek-chat`（zh→en 中文原生、$0.14/M tokens）。**Latency 后备**：DeepSeek 从中国大陆 p50 ~200ms，从欧美网络 p50 400-800ms，demo 机网络环境下若实测 p50 >500ms，切到 Groq Llama 或 Cerebras（零成本快、英文翻译质量略低）；DeepSeek **prefix cache** 命中时延迟更低（注意：cache 是按 prompt 前缀命中的，不是按"句子"命中。我们的系统提示词每次都一样，所以系统提示词部分天然命中；用户内容部分每次不同，不命中）。Demo 前的"预热"实际意义是让冷启动 instance 有时间起来，不是缓存特定句子。
- **拼音 → 中文**：用 **`Pinyin2Hanzi`** (HMM-based reverse pinyin → Chinese, `pip install Pinyin2Hanzi`)。**不要用 pypinyin** — 它是 Chinese→pinyin 单向，没有反查。中文 composition 显示质量不重要 (LLM 一替换就走人)，但拼音→中文这步必须能跑通。
- **Hotkey**：默认 **Ctrl+Shift+T** (避开 Ctrl+Space — Ctrl+Space 是 Windows 默认 IME 切换 + IDE 自动补全 + 各家 app 的快捷键，必冲突)。`config.json` 可覆盖。
- **管理员权限**：TSF + COM 注册必需 admin。`install.bat` 自提权 (PowerShell `Start-Process -Verb RunAs` 包裹) 或显式提示用户右键"以管理员运行"。
- **重启**：每次 PIME 后端代码修改不一定要重启 Windows，但 COM 注册改动要至少重启 explorer.exe，部分 app (Office) 要完全重启。Hour-by-hour 预算每次 COM 改动 +15 min。
- **不投入**：本地推理 (ollama / llama.cpp)、候选词 UI、设置面板、多语言对、Tab tone 选择器、系统托盘 (Windows IME 指示器够用)、产品级 telemetry。

## Premises (agreed, with revision)

1. ✅ 真 OS 级 IME 是硬约束。要在所有 Windows app 工作，不是限定场景的覆盖层。
2. ✅ Pause-300ms + composition replace 比 commit-translate 更值得。即接受偶尔延迟尖刺、错误闪烁、节奏被打断换"魔法感"。
3. ✅ Demo 目标：zh→en 单一语言对。其它对 nice-to-have。
4. ✅ LLM 用云 API。本地小模型质量降级 + 工程量不划算。
5. ✅ **REVISED**: 路径是 fork **PIME (EasyIME/PIME)**，不是 Rime。
   - 原 premise: fork Rime + Lua + 外部 helper
   - 修正原因: Rime 核心是"按键序列查字典生成候选词"，我们不要候选 (LLM 就是候选生成器)；继续走 Rime 等于跟它的同步 Lua 模型 + 不存在的 timer + 不确定的 composition rewrite API 死磕，前 5 小时大概率发现要写 C++ helper + 命名管道 IPC，于是在 Rime 里复刻 PIME 架构。PIME 后端是 Python 进程，可以原生跑 asyncio + 调网络。
   - **关键 API 待 hour 0-2 验证**: PIME 的 `TextService` 有 `setCompositionString(str)` 类似的 API (查 PIME repo `python/server/textService.py` 实际签名)。如果该 API 在某些 host app 行为不一致，参考"Open Questions"里 Chrome / Electron 后备方案。这条不是 100% 确定，是高概率可行 — hour 1 的 echo IME 验证就为这个。

## Cross-Model Perspective

Subagent (Claude，未看对话仅看结构化总结) 冷读关键发现：

1. **Steelman**: 双向 shadow channel — 我打中文外发英文，朋友打英文我看到中文，原文用 Unicode tag chars 隐藏在 message body 里，Ctrl+hover 浮出。"两人都打母语，谁也不知道对方敲什么"。reframe 为 language-agnostic chat substrate。**承认**：48hr 内做不出来；保留为 v2。
2. **关键 quote**: 用户说 "**就**希望是输入法" 中的 "就" 字是排他性词。意思不是"输入法是个好选项"，是"非 IME 路径全是错的"。**结论**：demo magic 是普适性 (在每个 app 里都能用)，不是翻译质量。Demo 应优化为"5 个 app 切一遍"30 秒连拍。
3. **被采纳的 premise 修正**: PIME > Rime fork。用户接受。
4. **48hr 具体 prototype 蓝图**: PIME + Python + asyncio + DeepSeek + httpx + Pinyin2Hanzi。文件结构、hour-by-hour 预算、跳过项全部点名。被吸收为 Approach A。(注：subagent 原文写 pypinyin，是技术错误 — pypinyin 单向不能反查。本 doc 已切到 Pinyin2Hanzi。)
5. **被点名的 demo 杀手**: TSF composition 在 Chrome omnibox 和 Electron app 行为诡异。**Hour 1-2 必测**，不是 hour 30。

## Approaches Considered

### Approach A: Subagent 48hr Spec — PIME + DeepSeek + Pause-300ms (zh→en only)

PIME Python TSF backend。asyncio 300ms debouncer。DeepSeek API。Pinyin2Hanzi 反查显示 composition 中文（质量不重要，只是过渡态）。300ms 静止 → LLM 调用 → 英文替换 composition → 空格 commit。

- **Effort**: M (~30 hrs，48hr 内有 18hr buffer)
- **Risk**: Med (TSF 在 Chrome omnibox / Electron 的行为是 demo killer)

### Approach B: A + Tab 调语气 + 多语言对 config

A 全套 + Tab 循环切语气 (formal / casual / shorter / friendly) + 配置文件挑语言对 (zh→en, en→zh, en→ja, ...) + 系统托盘语言对指示。

- **Effort**: L (~50 hrs，超 4 小时)
- **Risk**: Med-High (每个额外功能 = 一个 demo bug 表面)

### Approach C: 双向 shadow channel — bilingual chat substrate

A 全套 + Unicode tag chars 隐写原文 + 浏览器插件 (WeChat web / Telegram desktop) 反向 LLM 还原 + websocket relay。两个朋友 demo。

- **Effort**: XL (~80 hrs)
- **Risk**: High (双 surface 同时可能挂；tag chars 可能被某些客户端剥；需朋友配合 demo)

## Recommended Approach: A

理由：
- "就希望是输入法" 的 "就" 字排他性 → demo magic 是普适性 → A 100% 撑住。
- B/C 都拿 demo polish 时间换功能。Hackathon 输 demo 就输全程。
- A 留 ~18 小时 buffer 应对 unknown-unknown。Chrome omnibox TSF bug 大概率吃 3-4 小时。
- B 的 Tab tone 是好点子，留作 v2，A 跑通后第 49 小时加 30 分钟可补。
- C 的 steelman 美但需朋友配合 + 浏览器插件 + 反向调用，hackathon 评委 demo 不出来。

### Composition Window 状态机 (核心 UX)

**注意**：Windows TSF 下 composition window 由**宿主 app 渲染**，IME 只能提交 composition string + 简单属性 (下划线、颜色片段)。不能渲染动画 spinner / 任意像素 — "spinner" 在本设计里改用文本标记 (`...`) 表示挂起。颜色仅当宿主 app 支持 attribute 时生效，不支持的 app fallback 为单色下划线（多数 app 表现为灰下划线）。

```
状态 0: 空闲 (无 composition)
  按键 → 状态 1

状态 1: 拼音累积中
  Composition string: pinyin literal, 例 "nihao"
  300ms idle 触发 → 状态 2
  Buffer 长度 ≥40 字符 (没遇到 idle) → 强制触发 → 状态 2
  Esc → 清 buffer → 状态 0
  Backspace → 删一字符；buffer 空则 → 状态 0
  非拼音字符 (数字/标点/ASCII 字母) → 当前 buffer commit raw + 该字符 raw → 状态 0

状态 2: 拼音 → 中文 (本地, Pinyin2Hanzi)
  Pinyin2Hanzi 反查，composition string 替换为中文
  立即派发 LLM 异步请求 (loop.call_soon_threadsafe)
  → 状态 3

状态 3: 等 LLM
  Composition string: 中文 + " ..." 后缀作挂起标记
  新按键到 → 取消挂起 LLM Task (见下面 Threading 模型) → 中文丢弃 → 回状态 1，
            **触发的这个键就是新 pinyin buffer 的首字符**
  LLM 返回 → 状态 4
  网络错 / timeout 2s → 状态 5

状态 4: 英文就绪
  Composition string: LLM 返回的英文
  Space → commit 英文 + 不追加空格 → 状态 0
  Backspace → 删一英文字符；composition 空 → 状态 0
  Esc → 进入子状态 4b (英文取消，中文 pending)
  其它字符 → commit 英文 → 该字符成新 buffer 的首字符 → 状态 1

状态 4b: 英文取消，中文 pending
  Composition string: 当初的中文
  1.5 秒无操作 → auto-commit 中文 → 状态 0
  Space → 立即 commit 中文 → 状态 0
  Esc → 清 → 状态 0
  其它字符 → commit 中文 → 该字符成新 buffer 的首字符 → 状态 1

状态 5: 错误 (LLM 挂)
  Composition string: 中文 + " ⚠"
  无自动 commit。
  Space → 把中文当英文 commit (用户至少能发出去) → 状态 0
  Esc → 清 → 状态 0
  其它键 → commit 中文-as-English → 该键成新 buffer 的首字符 → 状态 1
```

**Commit 副作用 (任何状态 → 0)**：清 pinyin_buffer，取消所有挂起 LLM task，丢 LLM context (新段不延续上下文，节省 token + 简化状态)。

### File Structure

```
typeeverything-ime/
  pime-translator/
    __init__.py
    translator_ime.py     # PIME TextService 子类，PIME 回调线程
    debouncer.py          # 后台线程跑 asyncio loop, loop.call_later
                          # cancel via loop.call_soon_threadsafe(task.cancel)
    llm_client.py         # httpx.AsyncClient, DeepSeek /chat/completions
                          # provider 可切：deepseek / groq / cerebras
    pinyin_buffer.py      # 拼音累积 + Pinyin2Hanzi 反查
    state.py              # 状态机 (见上)
  prompts/
    zh_en.txt             # 见 LLM 调用规格
  config.json             # {api_key, provider, model, hotkey,
                          #  debounce_ms (300), max_buffer (40), timeout_s (2)}
  install.bat             # PowerShell Start-Process -Verb RunAs 自提权
                          # 失败时打印"右键以管理员运行"提示
  demo.md                 # 5-app 顺序 + 录屏脚本
  README.md
  %LOCALAPPDATA%/typeeverything-ime/usage.jsonl  # console-only 默认；--log 时启用
                          # schema: {ts, in_tok, out_tok, cost_usd, latency_ms,
                          # provider, app_name}
```

**Threading 模型 (subagent 漏点 — 必须显式)**：PIME 把 keyDown / keyUp / commitText 等回调跑在它的内部线程上，**不是 asyncio 线程**。术语区分：
- `concurrent.futures.Future` (跨线程) — 由 `run_coroutine_threadsafe` 返回，给 PIME 回调线程持有。
- `asyncio.Task` (loop 内) — 在 asyncio loop 内创建，`.cancel()` 必须在 loop 线程里调。

写法：

```python
# 启动时（一次）
import asyncio, threading
loop = asyncio.new_event_loop()
threading.Thread(target=loop.run_forever, daemon=True).start()

# PIME 回调线程派发 LLM 任务，并保留 Future 句柄以便取消
def schedule_translate(text):
    fut = asyncio.run_coroutine_threadsafe(translate_coro(text), loop)
    return fut  # concurrent.futures.Future

# 取消（在 PIME 回调线程内）
def cancel_pending(fut):
    # fut.cancel() 在 concurrent.futures.Future 上只能取消"还没开始 await
    # 的协程"。已经在跑的协程要拿到底层的 asyncio.Task 调 .cancel()。
    # 最干净的写法是用下面的 _current_task dict 模式。

# LLM 完成回写 composition string — PIME 的 setComposition 必须在 PIME 线程
# 调，所以协程末尾要用 PIME 提供的 schedule-on-pime-thread 机制（或我们自己
# 包一个队列 + PIME 主循环里 poll 队列）。
```

`current_task` 共享 dict 模式 (推荐，最简单可靠):

```python
_current_task = {"task": None}

async def translate_coro(text):
    _current_task["task"] = asyncio.current_task()
    try:
        result = await llm_call(text)
        return result
    finally:
        _current_task["task"] = None

# 取消：
loop.call_soon_threadsafe(lambda: _current_task["task"] and _current_task["task"].cancel())
```

### LLM 调用规格

- Endpoint: `https://api.deepseek.com/chat/completions`
- Model: `deepseek-chat`
- Temperature: 0.3 (确定性优先于多样性)
- System prompt: `Translate the following Chinese to natural conversational English. Output only the translation, no quotes, no explanation.`
- User message: composition window 当前中文 string
- 单次往返 / debounce 触发
- 取消: 新按键到 → `asyncio.CancelledError`
- Timeout: 2s 硬上限 → 状态 5

## Open Questions (剩下真正未决的)

1. **TSF composition rewrite 在 Chrome omnibox 和 Electron 的行为**。Hour 1-2 必测。**预先承诺**：若 5 个 demo app 中 ≥2 个挂掉，从 demo 列表里移除挂掉的 app，缩到剩余 N 个 app 演 — 不在挂掉的 app 上死磕。后备技术方案 (SendInput delete + insert) 留作 v2，hackathon 不投。
2. **DeepSeek p50 在 demo 机网络环境的实测延迟**。Hour 6-14 内必须实测。p50 >500ms 切 Groq；p50 >800ms 必须切。Demo 录屏前可对常用句做 prompt-cache 预热（同样的句子第二次明显更快，DeepSeek 支持 cache）。
3. **Pinyin2Hanzi 输出质量与"composition 显示中文"的可读性**。如果反查出的中文词序不对（HMM 偶尔），用户在 300ms 静止前看到怪中文，体验失分。**降级方案**：composition 状态 1 时直接显示拼音 literal，状态 2 跳过中文中间态，直接调 LLM (LLM 接受 pinyin 输入，输出英文)。这反而更快（少一次本地处理）但失去"中文 → 英文 魔法变身"的视觉。Hour 6-14 实测后决定走哪条。
4. **API key 在 demo 设备的存放**。Hackathon: `config.json` 明文，不进 git。产品化: 留 v2。
5. **PIME 输入法图标在 Windows IME 列表里如何显示**。需配置 manifest，PIME README 有例子，hour 20-30 阶段处理。

(已关闭：commit 后追加空格 = 不追加；Backspace / Esc 行为 = 见状态机；多段不延续上下文 = 见 commit 副作用；长拼音串无 idle = 状态 1 的 max_buffer=40 强制触发。)

## Risk Register

| # | 风险 | 概率 | 影响 | 触发时机 | 缓解 / 触发后预案 |
|---|------|------|------|----------|--------------------|
| R1 | TSF composition rewrite 在 Chrome omnibox 不工作 | High | demo 砍一个 app | Hour 1-2 smoke test | 移除 Chrome 地址栏 from demo list，改用 Chrome 普通页面文本输入 |
| R2 | TSF composition rewrite 在 Electron 不工作 | Med | demo 砍 1-2 个 app | Hour 1-2 | Open Q #1 — 5 中 ≥2 挂就缩 demo 列表 |
| R3 | DeepSeek p50 demo 机网络 >500ms | Med | demo 节奏迟钝 | Hour 6-14 实测 | 切 Groq Llama (免费快) / Cerebras |
| R4 | Pinyin2Hanzi 输出质量差 | Med | composition 中文不可读 | Hour 2-6 | 跳过中间态，直接 pinyin literal → LLM (LLM 直接吃拼音) |
| R5 | PIME `setCompositionString` 签名 / 行为偏离假设 | Med | 状态机重写 | Hour 0-2 | Hour 2-4 buffer 已预留 |
| R6 | LLM 取消挂起请求线程模型出 bug | Med | 串扰 / 旧请求结果污染 composition | Hour 6-14 | 共享 dict + `loop.call_soon_threadsafe` 模式（File structure 章节代码示例） |
| R7 | install.bat 自提权失败 | Low | 用户路径中断 | Hour 30-40 | README 显式说"右键以管理员运行" |
| R8 | API key 在 demo 时不小心录进屏幕 | Low | 安全 / 钱被刷 | 录屏前 | API key 放 `config.json`、demo 录屏前清空终端 + 关掉 logger |
| R9 | 评委网络比 demo 机差 | Low | 现场调用慢 | Demo 当天 | 录屏放 demo 当主轴，现场补一段；prompt-cache 不能救句子，但 instance 预热能少 1-2 秒冷启动 |

每个风险必须在指定的 hour 内主动验证或暴露。**Hour 1-2 是最关键的 24 小时缩小决策点** — R1/R2/R5 都在此关头决定后续架构。

## Success Criteria

Demo 录像满足：
- [ ] 在 5 个不同 Windows app 里成功 type→pause→translate→commit，单次循环 <2s
- [ ] 至少一次 demo 在 Chrome 地址栏 (因为这是评委最熟悉的 app)
- [ ] 至少一次 demo 在 Notepad (普适性证明)
- [ ] 总 demo 长度 ≤30 秒，节奏紧凑
- [ ] 网络一次失败时 fallback 到中文 commit 看得见、不卡死

代码满足：
- [ ] `git clone && install.bat (右键管理员) && 配 config.json API key && 重启 explorer → 直接能用`
- [ ] 状态机所有状态都有可见反馈（pinyin literal / 中文 / 中文+`...` / 英文 / 中文+`⚠`）

Stretch (不算 must-have，时间多再做)：
- [ ] usage.jsonl token 计数 + cost log
- [ ] config.json hot-reload
- [ ] 系统托盘图标 (Windows 自带 IME 指示器够用，托盘是锦上添花)

## Demo Script (具体句子)

5-app 录屏顺序，每 app 一句中文，30 秒总长，每句目标循环 <6 秒（敲入 + 300ms idle + LLM + commit）：

| # | App | 中文输入 | 期望英文 | 备注 |
|---|-----|----------|----------|------|
| 1 | Notepad | `nihao shijie` → "你好世界" | "Hello world" | 普适性 anchor — 最朴素的 app 也能用 |
| 2 | Chrome 地址栏 | `bangwoyixiazhege buwentijixufu` → "帮我一下这个补丁是不是修复" | "Help me check if this patch fixes the issue" | 地址栏是评委最熟悉的"输入"位 |
| 3 | Discord 文本框 | `mingtianwojiapeixiaohaiquhaibian` → "明天我家陪小孩去海边" | "Taking my kid to the beach tomorrow" | 跨语言聊天主场景 |
| 4 | VSCode 注释 | `zhefangfajiekoubuanquan` → "这方法接口不安全" | "This method interface is unsafe" | 程序员场景 — 工作语言切换 |
| 5 | Word | `xinjihuabengfayouzhixingju` → "新计划本发有执行据" | "New plan with concrete execution roadmap" | 公司商务场景 |

录屏开头 1 秒打字幕"Type Chinese, send English. Same IME, every app." 结尾 1 秒"github.com/dai/typeeverything-ime"。

如果 R1/R2 触发，按以下顺序砍：先砍 5 (Word，最少惊喜)，再砍 4 (VSCode，开发者眼里普通)。Notepad/Chrome/Discord 是底线，至少留 3 个。

## Distribution Plan

Hackathon scope：
- GitHub Releases 发 `.zip`（PIME 后端 + install.bat + 配置模板）
- README 截图 + 30 秒 demo 录屏
- 用户路径：clone → 填 API key → install.bat (右键管理员) → 重启 explorer 或登出 → 在 Windows 输入法列表里切到本 IME (任务栏语言指示器或 Win+Space)

后续（不在 48hr 范围）：
- 签名 .msi installer (PIME 不需要管理员就能装，但 TSF 注册需要管理员，msi 帮跳)
- API key 引导 (GUI 一次性配置)
- DeepSeek API 注册引导 (因为这才是用户买帐户的真实摩擦点)
- 双语模型托管 (考虑 v2 自营 backend，避开用户管 key)

## Next Steps (hour-by-hour, 48hr budget)

参照 subagent 推荐：

- **Hour 0-2**: 装 PIME (admin)，跑通 echo IME sample。**关键测试**: 在 Notepad / Word / Chrome 地址栏 / Discord / VSCode 都试 — composition rewrite 在哪个 app 不工作？提前发现。**确认 PIME `TextService.setCompositionString` 真实签名 + 行为** (查 PIME repo `python/server/textService.py`)。每次 COM 改动 +15min 重启 explorer 预算。
- **Hour 2-4 (buffer)**: 如果 PIME composition API 名字 / 行为偏离假设 (如 `setCompositionString` 不存在或行为异常)，这两小时用来读 PIME 源码 + 改状态机适配真实 API。如果一切顺利，这两小时给 hour 2-6 任务做提前量。
- **Hour 2-6**: pinyin buffer + composition preview。`pip install Pinyin2Hanzi`（**不是 pypinyin**，pypinyin 不能反查）。打 `nihao` 看到 `你好`，空格 commit `你好`。先不接 LLM。如果 Pinyin2Hanzi 输出质量差到看不下去，启动备选：状态 1 不转中文、直接显 pinyin literal、状态 2 跳过、直接送 LLM `pinyin → english` (LLM 完全可以处理拼音)。
- **Hour 6-14**: debouncer (后台 asyncio loop + `loop.call_later`) + LLM 调用 (httpx.AsyncClient DeepSeek)。**实测 demo 机 p50 延迟** — >500ms 切 Groq。Composition 替换中文为英文。空格 commit 英文。
- **Hour 14-20**: 错误状态。Timeout 2s → 状态 5。新按键 → `loop.call_soon_threadsafe(task.cancel)`。空响应 → no-op。Esc / Backspace 实现。Max-buffer-40 强制触发。
- **Hour 20-30**: Ctrl+Shift+T 切换 + manifest 图标 + config.json hot-reload。
- **Hour 30-40**: **demo 打磨**。录 5-app 连拍。修哪个 app 出问题（大概率 Chrome 地址栏）。如果 ≥2 app 挂，按 Open Q #1 缩列表。**冷启动预热**：录屏前先发 1-2 条 dummy 请求让 DeepSeek instance 热起来 (并非"为特定句子缓存结果"，cache 是 prefix 命中，命中的是系统提示词部分)。
- **Hour 40-48**: unknown-unknown buffer + README + GitHub Release zip。

## The First Real Action (今天)

不是开始写代码。**两件事 — 都做，都不超过 2 小时合计**：

**1. 验证 PIME 能在你的 Win11 上跑起来**
- `git clone https://github.com/EasyIME/PIME` 到 D:\hrdai\
- 按官方 README 装一遍。**右键管理员运行 install.bat**，否则 COM 注册必失败。
- 切到 PIME 自带的 sample IME，在 Notepad 里打两个字。
- 同样测 Chrome 地址栏 / Word / VSCode / Discord。哪个 app 看不到 composition string？记下来。
- **关键**：找 `textService.py` (或类似)，确认 `setCompositionString` (或等价方法) 的真实方法名 + 签名。这决定了状态机能不能写。

**2. 验证 Pinyin2Hanzi 输出质量**
- `pip install Pinyin2Hanzi`
- 跑：`from Pinyin2Hanzi import DefaultHmmParams, viterbi; ...` 测 5 句常用：`nihao`, `wojiaoxiaoming`, `mingtiankaihui`, `wodelaobandui`, `bangmaikangdi`
- 如果 5 句里 ≥2 句出来不像中文，**今晚就要决定**走"拼音 literal → LLM" 备选路 (跳过 Pinyin2Hanzi)。

如果 1 做不通，所有后续白搭。如果 2 做不通，状态机简化但仍可演。**两条都先验证再写代码**。

## Quick Reference (复制粘贴用)

**API 注册** (DeepSeek)
- 网址: https://platform.deepseek.com
- 注册 → API Keys → 创建 key → 充值 $5 (够 demo 100x 反复跑)
- Key 放 `config.json` 的 `api_key` 字段，不要进 git

**依赖安装**
```bash
pip install httpx==0.28.* Pinyin2Hanzi==0.1.*
git clone https://github.com/EasyIME/PIME D:\hrdai\third_party\PIME
```

**PIME 装机** (右键 cmd 以管理员运行)
```cmd
cd D:\hrdai\third_party\PIME
install.bat
```
然后**重启 Windows**或登出再登入。任务栏语言指示器里能看到 PIME。

**LLM 调用模板** (`llm_client.py` 主体)
```python
import httpx

DEEPSEEK_URL = "https://api.deepseek.com/chat/completions"
SYSTEM = "Translate the following Chinese to natural conversational English. Output only the translation, no quotes, no explanation."

async def translate(text: str, api_key: str, model="deepseek-chat", timeout=2.0):
    async with httpx.AsyncClient(timeout=timeout) as client:
        r = await client.post(
            DEEPSEEK_URL,
            headers={"Authorization": f"Bearer {api_key}"},
            json={
                "model": model,
                "temperature": 0.3,
                "messages": [
                    {"role": "system", "content": SYSTEM},
                    {"role": "user", "content": text},
                ],
            },
        )
        r.raise_for_status()
        return r.json()["choices"][0]["message"]["content"].strip()
```

**Pinyin → Chinese 模板** (`pinyin_buffer.py` 主体)
```python
from Pinyin2Hanzi import DefaultHmmParams, viterbi
hmm = DefaultHmmParams()

def pinyin_to_hanzi(pinyin_str: str) -> str:
    # 'nihao' → ['ni', 'hao']  按需要可以更聪明地分音
    syllables = split_pinyin(pinyin_str)  # 见下
    if not syllables:
        return pinyin_str
    paths = viterbi(hmm_params=hmm, observations=tuple(syllables), path_num=1)
    if paths:
        return ''.join(paths[0].path)
    return pinyin_str

def split_pinyin(s: str):
    # 简化版 greedy longest-match。完整版用 pypinyin 的音节字典或自己列。
    syllables = []
    i = 0
    while i < len(s):
        for length in (4, 3, 2, 1):  # 拼音音节最长 4 字符 (zhuang)
            cand = s[i:i+length]
            if cand in VALID_SYLLABLES:
                syllables.append(cand)
                i += length
                break
        else:
            return []  # 切不开
    return syllables
```

**Demo 录屏工具**
- Windows 自带: Win+G 打开 Xbox Game Bar → 录屏
- 推荐: ShareX (开源、热键定制) 或 OBS (帧率高、压片好)

## Glossary (术语)

- **IME** (Input Method Editor) — 输入法。Windows 上是个 COM 组件。
- **TSF** (Text Services Framework) — Windows 现代输入法接口，替代老 IMM32。本项目走 TSF。
- **IMM** (Input Method Manager) — Win32 老式输入法接口。本项目不用。
- **COM** (Component Object Model) — Windows 老式插件标准。TSF 用 COM。注册要 admin。
- **CLSID** — COM 组件的 GUID 标识符。
- **Composition window** — 输入法显示"待 commit 文本"的位置。**由宿主 app 渲染**，不是 IME 画的。
- **Composition string** — composition window 里的文本内容，IME 通过 TSF API 设置。
- **Commit** — 把 composition 内容确认提交到 app，成为 app 的实际输入文本。空格 / 回车通常触发 commit。
- **Candidate window** — 输入法显示候选词的弹窗 (搜狗里那个 1/2/3/4 候选)。本项目**不用**。
- **PIME** (Easy IME) — 开源 Python/Node TSF host。`https://github.com/EasyIME/PIME`。
- **Pinyin2Hanzi** — Python 库，HMM 反查 pinyin → 中文。`pip install Pinyin2Hanzi`。
- **pypinyin** — Python 库，正向中→拼。**不能反查**，不要用。
- **DeepSeek** — 中国 LLM 服务商。`deepseek-chat` 模型对中文友好、便宜、快。
- **httpx** — Python 异步 HTTP 客户端。本项目用 `AsyncClient`。
- **asyncio Task** — asyncio loop 内的协程对象，`.cancel()` 在 loop 内调。
- **concurrent.futures.Future** — 跨线程的 future，`run_coroutine_threadsafe` 返回这种。
- **prefix cache** — DeepSeek 等服务的 prompt 缓存。命中条件: prompt 前缀完全相同。系统提示词通常天然命中。
- **Pause-300ms 触发** — 本项目核心 UX。用户停止打字 300ms 自动触发 LLM 翻译。

## What I noticed about how you think

- 你说 "**就**希望是输入法"。这个 "就" 字 subagent 抓得对：你不是在说"IME 是好选项之一"，你是在排他性地否决所有非 IME 路径。即使你听了我列出来的浏览器插件路径（一天搞定）和独立 app 路径（不依附），仍然只挑 IME。这不是技术判断，是**审美判断** — 你心中那个产品长什么样很清楚，不愿意为省时间损失它。
- 你听到延迟问题、subagent 质疑 Rime 时不抗辩，要求 "再具体一点跟我解释"。这是好习惯：先听懂再做决定，不为面子守原 premise。**信号**: 这意味着将来 demo 时 evidence 反过来推翻 premise，你也愿意切。这条 hackathon 必备。
- 你选 Pause-300ms 而不是 commit-translate。后者更容易实现、风险更低；前者视觉冲击强但更脆。你选了视觉感更强的那个。**信号**: 你做的决定 demo-driven，不 ship-driven。这对 hackathon 是优势，对产品化是隐患（产品化要 ship-driven）— 记住区别。
- 你给 IME 这个长度的对话保持着耐心，没有催 "直接给代码"。这意味着你愿意在动手前先让设计成型。这条不常见。
